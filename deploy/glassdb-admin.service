[Unit]
Description=Glass Database admin (Streamlit)
After=network.target

[Service]
Type=simple
User=glassdb
Group=glassdb
WorkingDirectory=/opt/glassdatabase
EnvironmentFile=/opt/glassdatabase/.env
Environment=HOME=/opt/glassdatabase
ExecStart=/opt/glassdatabase/.venv/bin/streamlit run admin/app.py \
    --server.port 8501 --server.address 127.0.0.1 \
    --server.baseUrlPath admin \
    --server.headless true \
    --server.enableCORS false \
    --browser.gatherUsageStats false
Restart=always
RestartSec=3
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full

[Install]
WantedBy=multi-user.target
